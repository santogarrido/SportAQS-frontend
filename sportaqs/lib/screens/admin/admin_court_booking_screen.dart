import 'package:flutter/material.dart';
import 'package:sportaqs/models/court.dart';

class AdminCourtBookingScreen extends StatelessWidget {
  final Court court;

  const AdminCourtBookingScreen({super.key, required this.court});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}